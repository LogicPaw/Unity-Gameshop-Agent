# Unity 游戏配置 Agent：项目全过程记录

> 文档用途：项目需求、架构演进、阶段成果、测试证据与关键取舍的统一记录
> 项目状态：核心闭环已完成，进入整理与展示阶段
> 最近更新：2026-08-29

## 1. 项目概述

本项目实现了一个面向 Unity 游戏团队的商店配置生成 Agent。策划或运营人员可以使用自然语言描述商品，例如：

> 创建三个商品：一把剑售价 15 金币；三个盾牌售价 20 金币并标记 Best Value；100 金币售价 7 宝石。

系统会理解需求、匹配游戏资源主数据、应用明确的默认值、执行确定性业务校验，并在全部通过后生成固定九字段的 CSV。生成结果既可以由 Codex 等 AI 客户端通过 MCP 调用，也可以由普通员工通过 Gradio 页面使用，最终能够被 Unity 中的 CSV 解析器读取。

当前完整链路：

```text
自然语言需求
      ↓
Skill：提供游戏商店领域语义与提取规则
      ↓
LangGraph：组织提取、构建、校验和条件分支
      ↓
确定性工具：主数据查询、默认值、ID、校验、CSV 导出
      ↓
MCP Server：对外暴露 generate_shop_config
      ↓
├─ Codex 等 MCP 客户端
└─ Gradio 团队前端（MCP Client）
      ↓
Unity CsvOfferLoader 导入验证
      ↓
人工复核、修改并进入团队原有版本控制流程
```

## 2. 项目目标

项目不只用于证明“自然语言可以生成表格”，还要证明以下能力：

1. 能从游戏研发流程中选择范围合适、可以客观验证的 Agent 场景；
2. 能将大目标拆成多个可独立测试的小阶段，而不是一次生成大系统；
3. 能划分模型、Skill、LangGraph、确定性代码、MCP 和 UI 的职责；
4. 能通过正常路径、异常路径、自动化测试和 Unity 实测验收 AI 生成结果；
5. 能使用 Vibe Coding 加速实现，同时由开发者控制需求、架构、测试与交付边界；
6. 能把原型逐步发展为可供团队和外部 AI 客户端复用的游戏流程自动化能力。

## 3. 阶段 00：需求分析与范围设计

### 要解决的问题

最初目标是制作一个面向游戏行业的自动填表 Agent，但“自动填表”范围过大，无法清楚评价正确性，也容易为了展示工作量而引入不必要复杂度。

### 本阶段完成内容

- 将场景收敛为 Unity 虚拟商店配置生成；
- 确认先验证自然语言到配置表，再逐步加入 Agent、MCP 和前端；
- 明确每个阶段必须可以单独运行、单独测试、单独演示；
- 建立轻量项目记忆方式：使用 Markdown 记录阶段目标、成果和取舍；
- 明确 Vibe Coding 中开发者与 AI 的职责边界。

### 为什么选择虚拟商店

- 属于真实的游戏策划、运营和 LiveOps 流程；
- 字段数量有限，但包含资源 ID、数量、价格、货币、分类和展示文案；
- 可以测试“玩家获得什么”和“玩家消耗什么”的语义方向；
- 可以使用 Unity 官方公开样例作为可复现的测试依据；
- 后续可以扩展到礼包、签到、活动或 Battle Pass，但 MVP 不提前支持这些场景。

### 阶段结论

首先验证最小、最关键的假设：系统能否把游戏需求稳定地转换为正确配置。MCP、Gradio、SVN 和复杂 Unity 集成不能在核心能力尚未验证时提前堆入。

## 4. 阶段 01：数据契约与 Unity 格式冒烟验证

### 要解决的问题

在开发 Agent 之前，先确认目标 CSV 的字段和格式确实可以进入 Unity。否则即使自然语言解析成功，也可能生成一个下游无法消费的结果。

### 数据来源

使用 Unity Technologies 公开的 Unity Gaming Services Virtual Shop 示例固定快照：

- 资源 ID：`COIN`、`GEM`、`PEARL`、`STAR`、`SWORD`、`SHIELD`；
- 资源类型：Currency 与 Inventory Item；
- 原始快照：`data/raw/unity_virtual_shop/official_snapshot.json`；
- 中文查询别名：`data/reference/resource_aliases.zh-CN.json`。

中文别名只负责把“金币”“宝石”“剑”“盾牌”等用户表达映射到正式 ID，不允许创造主数据中不存在的资源。例如“水晶剑”不会被自动编造成新 ID。

### 九字段数据契约

| 字段 | 职责 | 来源 |
|---|---|---|
| `offer_id` | 售卖项唯一 ID | 确定性代码生成 |
| `display_name` | 人类可读名称 | 用户输入或确定性规则生成 |
| `category` | 商品分类 | 根据奖励资源类型推断 |
| `reward_id` | 玩家获得的资源 ID | 从需求提取并查询主数据 |
| `reward_amount` | 玩家获得数量 | 从需求提取，缺失时默认 1 |
| `cost_id` | 玩家消耗的货币 ID | 从需求提取并查询主数据 |
| `cost_amount` | 玩家消耗数量 | 从需求提取，必须提供 |
| `badge_text` | 可选展示标签 | 从需求提取，缺失时为空 |
| `enabled` | 是否启用 | 用户指定或默认 `true` |

### Unity 冒烟测试

先人工制作一行 CSV，再使用 C# `CsvOfferLoader` 在 Unity 中读取。Unity 控制台正确输出：

```text
Offer ID: VIRTUAL_SHOP_1_SWORD_FOR_COINS
Reward: SWORD x1
Cost: COIN x15
Enabled: True
```

### 阶段结论

这一测试只证明数据格式和字段语义能够被 Unity 读取，不等于完成生产级 Unity 集成。但它以最低成本排除了“Agent 最终输出无法进入 Unity”的基础风险，因此可以开始开发配置生成核心。

## 5. 阶段 02：确定性数据结构与工具

### 要解决的问题

如果直接让语言模型生成正式 CSV，模型可能创造资源 ID、混淆奖励与价格、遗漏字段或输出不稳定格式。因此先建立一个不依赖 Agent 的确定性内核，让人可以通过结构化输入完成整个过程。

### 本阶段完成内容

- `CandidateOffer`：保存模型可能提取到的不完整候选数据；
- `ShopOffer`：保存校验通过的正式九字段商品；
- `ResourceCatalog`：读取官方资源快照并处理名称和中文别名查询；
- `OfferConfigTools`：应用默认值、生成 ID、检查字段、阻止错误并导出 CSV；
- 命令行入口：支持使用人工编写的 JSON 生成 CSV；
- 固定输出列名和列顺序，保证 Unity 解析器可以稳定消费。

### 关键规则

- 数量和价格必须是正整数；
- `cost_id` 必须是 Currency 类型；
- 未知资源不允许自动创建；
- 重复 `offer_id` 会阻止导出；
- 正式 ID 由确定性规则生成，不交给模型自由发挥；
- 使用过的默认值会单独记录，不能伪装成用户明确提供的内容；
- 只要存在阻断错误，就不生成正式 CSV。

### 阶段结论

核心业务能力先被实现为可测试的普通程序。后续接入 Skill、LangGraph、MCP 或 Gradio 时，都只复用它，不重新实现业务规则。

## 6. 阶段 03：Skill 与 LangGraph Agent

### 要解决的问题

确定性工具只能处理结构化输入，还不能理解策划的自然语言。本阶段加入语言模型，同时避免让模型接管正式数据生成。

### Skill 的职责

项目内的 `unity-virtual-shop-config` Skill 负责：

- 解释商店需求中的奖励、价格、数量、货币和标签；
- 规定字段提取规则和正反例；
- 保持奖励与消耗方向正确；
- 缺失字段保留为空，不替用户编造；
- 要求模型只输出 `CandidateOffer`，不直接生成正式 ID 或 CSV。

### LangGraph 的职责

LangGraph 将一次任务组织为可观察的条件工作流：

```text
extract：模型结合 Skill 提取候选商品
   ↓
build：确定性工具查询主数据、应用默认值并校验
   ↓
是否存在错误？
   ├─ 是 → clarify：返回补充问题，不生成 CSV
   └─ 否 → export：生成固定 Schema CSV
```

在这个项目中，LangGraph 的价值不是增加节点数量，而是让“校验失败不能导出”成为明确的流程分支。

### 模型与代码的责任边界

| 模型负责 | 确定性代码负责 |
|---|---|
| 理解自然语言 | 主数据真实性检查 |
| 提取候选字段 | 字段类型和正整数校验 |
| 识别语义歧义 | 默认值、ID 和分类生成 |
| 形成待确认问题 | 重复检查和 CSV 导出 |

### 验证结果

- 单条和多条正常需求能够生成正式 CSV；
- “水晶剑”产生 `UNKNOWN_REWARD`；
- 未说明货币类型产生 `MISSING_CURRENCY`；
- 异常输入进入澄清分支且不创建正式输出文件；
- LangGraph 生成的多条 CSV 已由 Unity 实际读取。

## 7. 阶段 04：MCP 能力标准化

### 要解决的问题

命令行 Agent 只能由了解项目运行方式的人使用，其他 AI 客户端无法通过统一协议发现和调用这项能力。

### 本阶段完成内容

建立本地 STDIO MCP Server，对外暴露一个聚合工具：

```text
generate_shop_config(requirement, output_name)
```

MCP Tool 内部继续调用同一个 LangGraph Agent，并返回稳定的结构化结果：

- `status`；
- `offers`；
- `issues`；
- `defaults_applied`；
- `clarification_questions`；
- `output_path`。

同时增加输出文件名限制和服务端固定输出目录，避免调用方写入任意路径。

### 实际验证

- 使用 MCP Client 先执行 `list_tools`，确认工具可以被协议发现；
- 使用 `call_tool` 调用 `generate_shop_config`；
- 将 MCP Server 注册到 Codex；
- Codex 成功通过 `unity-shop-config` 生成三条商品 CSV；
- 实际输出保存在 `outputs/mcp/`。

### 阶段结论

Skill 和 LangGraph 构成 Agent 的内部智能与流程，MCP 负责把完整能力封装成标准接口。接入 MCP 的兼容客户端不需要了解项目内部类、提示词或节点实现。

## 8. 阶段 05：Gradio 团队前端

### 要解决的问题

MCP 解决了 AI 客户端复用问题，但普通策划或运营人员仍需要一个无需命令行、无需理解 MCP 的入口。

### 本阶段完成内容

使用 Gradio Blocks 创建轻量页面，提供：

- 自然语言商品需求输入；
- 安全的 CSV 输出文件名；
- 生成与校验按钮；
- 九字段正式商品预览；
- 默认值、错误和补充问题展示；
- 成功 CSV 下载。

### 调用链纠正

最初实现曾准备让 Gradio 直接调用 LangGraph。经过架构复核后，将调用链调整为：

```text
Gradio → MCP Client → MCP Tool → LangGraph → Skill + 确定性工具
```

这样 Gradio 与 Codex 都通过同一个 MCP 契约使用能力，前端不依赖 Agent 内部实现。当前 MVP 使用同进程 MCP 传输以降低部署复杂度；未来改成独立的 Streamable HTTP MCP 时，前端展示逻辑可以保持不变。

### 阶段结论

系统完成了两种真实使用入口：AI 客户端通过 MCP 调用，普通团队成员通过 Gradio 页面输入、预览和下载。

## 9. 阶段 06：Unity 端到端验证

### 要解决的问题

自动化测试只能证明 Python 侧结果符合预期，还需要证明 Agent 实际生成的 CSV 可以进入目标游戏引擎。

### 本阶段完成内容

- 使用 C# 编写轻量 `CsvOfferLoader`；
- 将 Agent/Gradio 生成的 CSV 放入 Unity；
- 在 Inspector 中指定 CSV 文件；
- 运行场景并在 Console 检查解析结果；
- 验证剑、盾牌和货币兑换等多条商品；
- 验证 `offer_id`、奖励 ID、奖励数量、货币 ID、价格和启用状态。

### 阶段结论

项目已经形成“自然语言 → Agent → MCP/Gradio → CSV → Unity”的可演示闭环。当前 Unity 代码是格式验证型解析器，不宣称已经替代目标公司的正式配置管线。

## 10. 测试与验收

当前共有 19 项自动化测试，覆盖：

- 官方资源快照与中文别名查询；
- 未知资源禁止编造；
- 单条、多条与货币兑换正常路径；
- 默认值应用；
- 非正数、非货币成本和重复 ID；
- 固定九字段 CSV Schema；
- LangGraph 正常导出与澄清分支；
- MCP 工具发现和结构化调用；
- Gradio 输出映射、危险文件名阻止；
- Gradio 确实通过 MCP Tool 到达 Agent，而不是绕过 MCP。

测试分为三层：

```text
领域测试：资源、默认值、ID、数值、重复和 CSV
集成测试：LangGraph 分支、MCP 协议、Gradio → MCP
人工验收：Codex MCP 调用、Gradio 页面、Unity 运行时解析
```

这套测试方式体现了 Vibe Coding 的核心要求：AI 可以加速实现，但生成代码和系统行为必须由明确的验收标准约束。

## 11. 各技术组件的分工

| 组件 | 解决的问题 | 不负责什么 |
|---|---|---|
| Pydantic 数据模型 | 定义候选数据、正式数据和错误契约 | 不理解自然语言 |
| ResourceCatalog | 提供可信资源主数据和别名映射 | 不创造新资源 |
| 确定性工具 | 默认值、ID、分类、校验、CSV | 不判断模糊语义 |
| Skill | 封装游戏商店领域语义和提取规范 | 不执行硬性业务校验 |
| LangGraph | 管理状态、节点和导出/澄清分支 | 不代替具体业务规则 |
| MCP | 将完整能力开放为标准工具 | 不重新实现 Agent |
| Gradio | 提供普通员工可使用的 Web 界面 | 不直接依赖 Agent 内部代码 |
| Unity C# Loader | 验证生成配置能被游戏项目消费 | 不负责生成或版本管理 |

## 12. Vibe Coding 开发方式

### 开发者负责

- 选择业务场景并不断收敛范围；
- 决定从小到大的开发顺序；
- 定义九字段数据契约和字段责任；
- 划分 Skill、LangGraph、工具、MCP 和 Gradio 的边界；
- 发现 Gradio 绕过 MCP 的架构问题并要求纠正；
- 设计正常路径、异常路径和 Unity 验收方式；
- 决定不继续扩展低收益的 SVN 自动提交。

### AI 协助

- 补充实现细节与工程风险；
- 生成数据模型、工具、节点、MCP 和 UI 的初始代码；
- 根据架构反馈重构调用链；
- 编写与运行自动化测试；
- 定位环境、依赖和进程占用问题；
- 维护项目说明与阶段记录。

### 核心原则

Vibe Coding 不等于接受 AI 一次生成的大系统。本项目采用“小步实现 → 独立测试 → 人工理解 → 发现偏差 → 调整架构 → 回归验证”的方式，AI 提高编码速度，开发者保持对产品目标、系统边界和最终质量的控制。

## 13. 关键决策与取舍

| 决策 | 原因 |
|---|---|
| 使用 CSV 而不是继续追求复杂 Excel | CSV 更容易 Diff、被 Unity 消费，也足以验证核心技术 |
| 先人工验证一行 CSV 再开发 Agent | 以最低成本验证下游格式可行性 |
| 模型只生成候选数据 | 避免模型直接控制正式 ID、校验和文件格式 |
| 未知资源必须澄清 | 防止模型幻觉进入游戏配置 |
| 使用 LangGraph 条件分支 | 明确保证错误数据不能进入导出节点 |
| MCP 只暴露一个聚合工具 | MVP 先展示完整能力复用，不提前拆分大量接口 |
| Gradio 通过 MCP 调用 | 让 Codex 与传统 Web 前端复用相同的公开契约 |
| 不开发自动 SVN 提交 | 自动提交与人工下载后提交收益差异小，且增加冲突、权限和误提交风险 |
| 保留人工复核和修改 | 游戏配置进入版本库前需要业务人员承担最终确认责任 |
| 暂不实现并发和多 Agent | 当前质量、可解释性和完整闭环比吞吐量更重要 |

## 14. SVN 与项目边界

项目早期曾计划加入 SVN 自动化。完成 MCP、Gradio 和 Unity 验证后重新评估，最终决定不实现自动 SVN 提交。

当前合理边界是：

```text
Agent 生成并校验 CSV
→ 用户在 Gradio 预览和下载
→ 用户按需要人工修改
→ Unity 验证
→ 使用公司已有 SVN 流程提交
```

这个决定不是功能缺失，而是对收益与风险的主动权衡。Agent 解决模糊需求到可靠结构化配置的问题，SVN 继续解决版本历史、冲突和团队协作问题。

## 15. 演示与证据索引

| 证据 | 文件 | 说明 |
|---|---|---|
| LangGraph 单条正常生成 | `手动保存/test1.png` | 自然语言生成正式九字段配置 |
| LangGraph 多条批量生成 | `手动保存/test2.png` | 批量商品和货币兑换 |
| 异常与澄清分支 | `手动保存/test3.png` | 未知资源、缺少货币时阻止导出 |
| MCP 注册 | `手动保存/mcp1.png` | Codex 中启用 `unity-shop-config` |
| MCP 实际调用 | `手动保存/mcp2.png`、`mcp3.png` | Codex 发现并调用工具生成 CSV |
| Gradio 三条配置 | `手动保存/gradio.png` | 输入、预览、默认值和下载 |
| Gradio 批量配置 | `手动保存/gradio2.png` | 五条配置的批量展示 |
| Unity 导入结果 | `手动保存/unity.png` | Unity Console 成功解析生成数据 |

用于正式展示时应裁掉无关侧边栏、本地用户名和早期命令错误。建议主线只保留 `gradio.png`、`mcp3.png`、`test3.png` 和 `unity.png` 四张图。

## 16. 当前成果

项目目前已经具备：

- 真实 Unity 示例主数据；
- 固定且可解释的九字段配置契约；
- 不依赖模型的确定性配置内核；
- 游戏商店领域 Skill；
- 带正常/澄清分支的 LangGraph Agent；
- OpenAI 兼容模型接口接入；
- 可被 Codex 调用的 STDIO MCP Server；
- 通过 MCP 使用能力的 Gradio 前端；
- Unity C# CSV 解析验证；
- 19 项自动化测试；
- 正常、批量、错误、MCP、前端和 Unity 的截图证据。

项目核心闭环已经完成，不需要继续添加 SVN、并发、数据库或复杂 Unity Editor 功能来证明其成立。

## 17. 后续工作

后续重点从“继续增加功能”转为“整理和表达”：

1. 清理 README，使运行方式和当前架构与本文一致；
2. 绘制一张简洁的项目架构图；
3. 裁剪并整理四张主要演示截图；
4. 清理公开仓库中的 `.env`、本地路径、缓存和无关输出；
5. 准备简历项目描述与 3 分钟面试讲解；
6. 如果面试官追问生产化方向，再讨论 HTTP MCP、权限、审计、多模板和并发，而不在 MVP 中提前实现。

## 18. 一句话总结

本项目通过渐进式 Vibe Coding，将一个自然语言填表想法发展为经过主数据约束、确定性校验、LangGraph 编排、Skill 增强、MCP 标准化、Gradio 交付并由 Unity 实际验证的游戏配置 Agent，同时保留人工复核和版本控制边界。
